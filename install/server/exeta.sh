# get a list of running task instances by executor
# update run status of each running task instance from the list with local status taken from its executor by checker executor type method
# get a list of running or submited task instances with fulfilled start condition with the highest importance for each executor
# get a sum of capacities of all running task instances for each executor
# get a capacity of each executor
# 
